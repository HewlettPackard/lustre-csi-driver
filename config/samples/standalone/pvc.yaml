apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-example
  namespace: lustre-bluster
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: lustre-fs
  volumeName: pv-example
  resources:
    requests:
      storage: 1Gi
