apiVersion: kustomize.config.k8s.io/v1
kind: Kustomization
namespace: lustre-csi-system

resources:
- namespace.yaml
- driver.yaml
- plugin.yaml

# To add image pull secrets, apply plugin_imagepullsecret_patch.yaml as an overlay patch.

images:
- name: controller
  newName: ghcr.io/hewlettpackard/lustre-csi-driver
  newTag: master
